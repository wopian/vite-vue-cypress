{
  "name": "vite-project",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "cypress": "cypress open --component --browser firefox",
    "cypress:run": "cypress run --component --browser firefox"
  },
  "dependencies": {
    "vue": "^3.2.37"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^3.0.1",
    "cypress": "^10.3.1",
    "typescript": "^4.7.4",
    "vite": "^3.0.3",
    "vue-tsc": "^0.39.0"
  }
}
